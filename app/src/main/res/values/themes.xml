<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme (light mode) -->
    <style name="Base.Theme.Challenge3" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/my_light_primary</item>
        <item name="colorOnPrimary">@color/my_light_onPrimary</item> <!-- Xanh lá nhạt -->
        <item name="colorSurface">@color/white</item>
        <item name="colorOnSurface">@color/black</item>
        <item name="android:statusBarColor">@color/my_light_onPrimary</item> <!-- Tránh che khuất -->
    </style>

    <style name="Theme.Challenge3" parent="Base.Theme.Challenge3" />
</resources>